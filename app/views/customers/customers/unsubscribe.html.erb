<div class="container">
  <div class="withdraw-container text-center mt-5">
    <div class="row justify-content-center">
      <h4>本当に退会しますか？</h4>
    </div>

    <div class="row justify-content-center">
      <h5>退会すると、会員登録情報やこれまでの購入履歴が閲覧できなくなります。<br>退会する場合は、「退会する」をクリックしてください。</h5>
    </div>

    <div class="row justify-content-center">
     <%= link_to "退会しない", customers_customer_path(current_customer.id), class: "btn btn-primary " %>
     <%= link_to "退会する", customers_withdraw_path(current_customer.id), method: :patch, class: "btn btn-danger mx-5" %>
    </div>
  </div>
</div>